#include "config.h"

// The run_standalone() function runs the program in standalone mode, sending
// packets to a destination and analyzing the response to detect compression.
void run_standalone(struct Config *config);
